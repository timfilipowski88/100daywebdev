const myName = 'Max';

if (myName === 'Max') {
  console.log('Hello');
}

let isLoggedIn = true;

if (isLoggedIn) {
  console.log('User is logged in!')
}

if (!isLoggedIn) {
  console.log('User is NOT logged in!')
}